ensure platform support
    using $TARGET_PLATFORM to check support in manifest

ensure depedencies version
    use git describe --tags to get version

ensure callback inplementation
    grep for the function declaration in app src

