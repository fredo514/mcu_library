#ifndef BOOTLOADER_H
#define	BOOTLOADER_H

#include <stdint.h>
#include "error.h"

// communication interface: SD card, UART, USB, I2C, etc
// memory interface: internal flash, but also external EEPROM or file system, etc
// scheduling or at least timing

#endif // BOOTLOADER_H